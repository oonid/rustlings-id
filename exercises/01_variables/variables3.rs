fn main() {
    // TODO: Ubah baris di bawah ini untuk memperbaiki error dari kompiler.
    // Variabel `x` sudah dideklarasikan dengan tipe `i32`, tetapi belum diberi nilai.
    // Rust tidak mengizinkan membaca nilai dari variabel yang belum diinisialisasi.
    // Kompiler akan memberikan error karena `x` belum memiliki nilai saat dicetak.
    // Petunjuk: berikan nilai awal pada `x` saat deklarasi.
    let x: i32;

    println!("Angka {x}");
}
